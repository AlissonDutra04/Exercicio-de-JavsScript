/*Faça um programa que solicite ao usuário que insira um número. O programa deve calcular a soma de todos os números de 1 até o número inserido pelo usuário e imprimir o resultado. */


/*// Solicita ao usuário que insira um número e converte a entrada para um número inteiro
let numero = parseInt(prompt("Insira um número:"));

// Inicializa a variável 'i' com o valor 1
let i = 1;

// Imprime os números de 1 até o número inserido pelo usuário
do {
  console.log(i);
  i++;
} while (i <= numero); */

let numero =parseFloat(prompt("Digite um número"));

let calcular_soma

let i = 1;

do {
  console.log(1)
  i++
} while (i)
